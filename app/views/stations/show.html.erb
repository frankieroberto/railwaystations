<% content_for :page_title, @station.name %>

<%= content_tag "article" do %>
  <%= content_tag "header" do %>

  <%= File.read("#{Rails.root}/app/assets/images/#{@station.id}.svg").html_safe %>

    <%= content_tag "hgroup" do %>
      <%= content_tag "h1", @station.name %>
      <%= content_tag "h2", @station.id %>
    <% end %>
  <% end %>




  <%= render "stations/short_section", { :title => t('things_to_see.html'), :content => @station.things_to_see } %>

  <%= render "stations/short_section", { :title => t('nearest_pub.html'), :content => @station.nearest_pub } %>

  <%= render "stations/short_section", { :title => t('meet_at.html'), :content => @station.meet_at } %>

  <%= render "stations/short_section", { :title => t('best_coffee.html'), :content => @station.best_coffee } %>

  <%= render "stations/short_section", { :title => t('lunch_to_go.html'), :content => @station.lunch_to_go } %>

  <%= render "stations/short_section", { :title => t('time_to_kill.html'), :content => @station.time_to_kill } %>

  <%= render "stations/short_section", { :title => t('luggage_storage.html'), :content => @station.luggage_storage } %>

  <%= link_to "<span class='arrow'></span> More stations".html_safe, root_path, :class => "back" %>


<% end %>